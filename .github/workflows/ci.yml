name: Run on VM (Manual or Daily)

on:
  schedule:
    - cron: '0 */5 * * *'      # Runs daily at 9 AM UTC
  workflow_dispatch:         # Allows manual runs via GitHub UI

jobs:
  build:
    runs-on: ubuntu-latest   # Runs on GitHub-hosted Ubuntu VM
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "🎉 Hello from GitHub Actions!"

